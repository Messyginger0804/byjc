{
  "compilerOptions": {
    "baseUrl": ".", // Add this to define the root directory for imports
    "paths": {
      "@/*": ["./src/*"], // Alias for src directory
      "contentlayer/generated": ["./.contentlayer/generated"] // Alias for Contentlayer
    }
  }
}
